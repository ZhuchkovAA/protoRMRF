// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v4.25.1
// source: apfish.v1/ship/summary/ship_summary.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "apfish.v1.ship.summary";

export interface ShipSummary {
  id: Long;
  imo: Long;
  typeId: Long;
  classId: Long;
  countryId: Long;
  name: string;
  mmsi: Long;
  callSignId: Long;
  callSignValue: string;
  dateBuild: Date | undefined;
  deadWeight: Long;
  tonnage: Long;
  createdAt: Date | undefined;
}

function createBaseShipSummary(): ShipSummary {
  return {
    id: Long.ZERO,
    imo: Long.ZERO,
    typeId: Long.ZERO,
    classId: Long.ZERO,
    countryId: Long.ZERO,
    name: "",
    mmsi: Long.ZERO,
    callSignId: Long.ZERO,
    callSignValue: "",
    dateBuild: undefined,
    deadWeight: Long.ZERO,
    tonnage: Long.ZERO,
    createdAt: undefined,
  };
}

export const ShipSummary = {
  encode(message: ShipSummary, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (!message.id.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.id);
    }
    if (!message.imo.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.imo);
    }
    if (!message.typeId.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.typeId);
    }
    if (!message.classId.equals(Long.ZERO)) {
      writer.uint32(32).int64(message.classId);
    }
    if (!message.countryId.equals(Long.ZERO)) {
      writer.uint32(40).int64(message.countryId);
    }
    if (message.name !== "") {
      writer.uint32(50).string(message.name);
    }
    if (!message.mmsi.equals(Long.ZERO)) {
      writer.uint32(56).int64(message.mmsi);
    }
    if (!message.callSignId.equals(Long.ZERO)) {
      writer.uint32(64).int64(message.callSignId);
    }
    if (message.callSignValue !== "") {
      writer.uint32(74).string(message.callSignValue);
    }
    if (message.dateBuild !== undefined) {
      Timestamp.encode(toTimestamp(message.dateBuild), writer.uint32(82).fork()).ldelim();
    }
    if (!message.deadWeight.equals(Long.ZERO)) {
      writer.uint32(88).int64(message.deadWeight);
    }
    if (!message.tonnage.equals(Long.ZERO)) {
      writer.uint32(96).int64(message.tonnage);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(106).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ShipSummary {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShipSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = reader.int64() as Long;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.imo = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.typeId = reader.int64() as Long;
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.classId = reader.int64() as Long;
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.countryId = reader.int64() as Long;
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.mmsi = reader.int64() as Long;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.callSignId = reader.int64() as Long;
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.callSignValue = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.dateBuild = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.deadWeight = reader.int64() as Long;
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.tonnage = reader.int64() as Long;
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShipSummary {
    return {
      id: isSet(object.id) ? Long.fromValue(object.id) : Long.ZERO,
      imo: isSet(object.imo) ? Long.fromValue(object.imo) : Long.ZERO,
      typeId: isSet(object.typeId) ? Long.fromValue(object.typeId) : Long.ZERO,
      classId: isSet(object.classId) ? Long.fromValue(object.classId) : Long.ZERO,
      countryId: isSet(object.countryId) ? Long.fromValue(object.countryId) : Long.ZERO,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      mmsi: isSet(object.mmsi) ? Long.fromValue(object.mmsi) : Long.ZERO,
      callSignId: isSet(object.callSignId) ? Long.fromValue(object.callSignId) : Long.ZERO,
      callSignValue: isSet(object.callSignValue) ? globalThis.String(object.callSignValue) : "",
      dateBuild: isSet(object.dateBuild) ? fromJsonTimestamp(object.dateBuild) : undefined,
      deadWeight: isSet(object.deadWeight) ? Long.fromValue(object.deadWeight) : Long.ZERO,
      tonnage: isSet(object.tonnage) ? Long.fromValue(object.tonnage) : Long.ZERO,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
    };
  },

  toJSON(message: ShipSummary): unknown {
    const obj: any = {};
    if (!message.id.equals(Long.ZERO)) {
      obj.id = (message.id || Long.ZERO).toString();
    }
    if (!message.imo.equals(Long.ZERO)) {
      obj.imo = (message.imo || Long.ZERO).toString();
    }
    if (!message.typeId.equals(Long.ZERO)) {
      obj.typeId = (message.typeId || Long.ZERO).toString();
    }
    if (!message.classId.equals(Long.ZERO)) {
      obj.classId = (message.classId || Long.ZERO).toString();
    }
    if (!message.countryId.equals(Long.ZERO)) {
      obj.countryId = (message.countryId || Long.ZERO).toString();
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (!message.mmsi.equals(Long.ZERO)) {
      obj.mmsi = (message.mmsi || Long.ZERO).toString();
    }
    if (!message.callSignId.equals(Long.ZERO)) {
      obj.callSignId = (message.callSignId || Long.ZERO).toString();
    }
    if (message.callSignValue !== "") {
      obj.callSignValue = message.callSignValue;
    }
    if (message.dateBuild !== undefined) {
      obj.dateBuild = message.dateBuild.toISOString();
    }
    if (!message.deadWeight.equals(Long.ZERO)) {
      obj.deadWeight = (message.deadWeight || Long.ZERO).toString();
    }
    if (!message.tonnage.equals(Long.ZERO)) {
      obj.tonnage = (message.tonnage || Long.ZERO).toString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShipSummary>, I>>(base?: I): ShipSummary {
    return ShipSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShipSummary>, I>>(object: I): ShipSummary {
    const message = createBaseShipSummary();
    message.id = (object.id !== undefined && object.id !== null) ? Long.fromValue(object.id) : Long.ZERO;
    message.imo = (object.imo !== undefined && object.imo !== null) ? Long.fromValue(object.imo) : Long.ZERO;
    message.typeId = (object.typeId !== undefined && object.typeId !== null)
      ? Long.fromValue(object.typeId)
      : Long.ZERO;
    message.classId = (object.classId !== undefined && object.classId !== null)
      ? Long.fromValue(object.classId)
      : Long.ZERO;
    message.countryId = (object.countryId !== undefined && object.countryId !== null)
      ? Long.fromValue(object.countryId)
      : Long.ZERO;
    message.name = object.name ?? "";
    message.mmsi = (object.mmsi !== undefined && object.mmsi !== null) ? Long.fromValue(object.mmsi) : Long.ZERO;
    message.callSignId = (object.callSignId !== undefined && object.callSignId !== null)
      ? Long.fromValue(object.callSignId)
      : Long.ZERO;
    message.callSignValue = object.callSignValue ?? "";
    message.dateBuild = object.dateBuild ?? undefined;
    message.deadWeight = (object.deadWeight !== undefined && object.deadWeight !== null)
      ? Long.fromValue(object.deadWeight)
      : Long.ZERO;
    message.tonnage = (object.tonnage !== undefined && object.tonnage !== null)
      ? Long.fromValue(object.tonnage)
      : Long.ZERO;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
